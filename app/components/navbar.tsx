export default function Navbar() {
  const styles = {
    title: 'font-bold text-2xl',
  };

  return (
    <section>
      <div id="title" className={styles.title}>
        Tome
      </div>
    </section>
  );
}
